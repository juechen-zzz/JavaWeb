# 系统级别的配置
spring:
  cloud:
    config:
      name: config-client   # 需要从 git 上读取的资源名称，不要后缀
      profile: dev          # 选择哪一个环境
      label: master         # 选择哪一个分支
      uri: http://localhost:3344    # 服务端的 uri